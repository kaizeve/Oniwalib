{
  "name": "oniwalib",
  "version": "0.0.0",
  "private": true,
  "description": "A native WhatsApp Multi-Device client, built on RTS (working name). Baileys-style: talks the socket directly, no browser.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "for f in wire wabinary noise auth socket; do bun test/$f.test.ts || exit 1; done",
    "test:rts": "for f in wire wabinary noise auth socket; do ../rts/target/fast/rts run test/$f.test.ts || exit 1; done"
  },
  "author": "loveless",
  "license": "SEE LICENSE IN LICENSE"
}
